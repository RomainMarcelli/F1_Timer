{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2020",  /* Version JavaScript cible */
    "module": "commonjs",  /* Système de module pour Node.js */
    
    /* Modules */
    "rootDir": "./src",  /* Répertoire des fichiers source TypeScript */
    "outDir": "./dist",  /* Répertoire de sortie des fichiers compilés JavaScript */
    
    /* Interop Constraints */
    "esModuleInterop": true,  /* Permet d'importer des modules CommonJS avec plus de flexibilité */
    "forceConsistentCasingInFileNames": true,  /* Enforce la casse correcte des fichiers importés */

    /* Type Checking */
    "strict": true,  /* Activer les options strictes pour améliorer la vérification des types */

    /* Completeness */
    "skipLibCheck": true  /* Ignore la vérification des fichiers de déclaration (d.ts) */
  },
  "include": ["src/**/*"],  /* Inclure tous les fichiers dans le dossier src */
  "exclude": ["node_modules"]  /* Exclure le dossier node_modules */
}
